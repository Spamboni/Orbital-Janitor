/**
 * styles.css
 * Orbital Janitor — UI chrome styling.
 * The game itself is rendered entirely on canvas.
 */

@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Share+Tech+Mono&display=swap');

/* ── Reset & base ─────────────────────────────────────────────────────────── */

*, *::before, *::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body {
  width: 100%;
  height: 100%;
  overflow: hidden;
}

body {
  background: #000;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-family: 'Share Tech Mono', 'Courier New', monospace;
  user-select: none;
  -webkit-user-select: none;
  touch-action: none;            /* prevent browser pan/zoom on canvas */
}

/* ── Layout ───────────────────────────────────────────────────────────────── */

#game-wrapper {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10px;
  width: 100%;
  max-width: 820px;
  padding: 0 8px;
}

/* ── Header ───────────────────────────────────────────────────────────────── */

#header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  padding: 0 2px;
}

#title {
  font-family: 'Orbitron', sans-serif;
  font-weight: 900;
  font-size: clamp(14px, 2.5vw, 22px);
  letter-spacing: 4px;
  color: #00e5ff;
  text-shadow: 0 0 10px #00e5ff, 0 0 30px #00e5ff44;
}

/* ── HUD ──────────────────────────────────────────────────────────────────── */

#hud {
  display: flex;
  align-items: center;
  gap: 18px;
}

.hud-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1px;
}

.hud-label {
  font-size: 8px;
  color: #334466;
  letter-spacing: 2px;
  text-transform: uppercase;
}

.hud-value {
  font-family: 'Orbitron', sans-serif;
  font-weight: 700;
  font-size: clamp(14px, 2vw, 20px);
  color: #00e5ff;
  text-shadow: 0 0 8px #00e5ff88;
  min-width: 2ch;
  text-align: center;
}

/* ── Reset button ─────────────────────────────────────────────────────────── */

#btn-reset {
  font-family: 'Orbitron', sans-serif;
  font-size: 9px;
  letter-spacing: 2px;
  color: #ff3060;
  background: transparent;
  border: 1px solid #ff3060;
  padding: 6px 12px;
  cursor: pointer;
  text-transform: uppercase;
  transition: background 0.15s, box-shadow 0.15s, color 0.15s;
  box-shadow: 0 0 8px #ff306044;
  -webkit-tap-highlight-color: transparent;
}

#btn-reset:hover,
#btn-reset:active {
  background: #ff306020;
  box-shadow: 0 0 18px #ff3060aa;
  color: #fff;
}

/* ── Canvas ───────────────────────────────────────────────────────────────── */

#canvas-wrap {
  position: relative;
  line-height: 0;          /* remove inline gap below canvas */
}

#canvas {
  display: block;
  border: 1px solid rgba(0, 175, 255, 0.14);
  box-shadow: 0 0 45px rgba(0, 200, 255, 0.12);
  cursor: crosshair;
  max-width: 100%;
  height: auto;
}

/* ── Win overlay (HTML, positioned over canvas) ───────────────────────────── */

#message-overlay {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: rgba(0, 0, 0, 0.45);
  pointer-events: none;

  opacity: 0;
  transform: scale(0.85);
  transition: opacity 0.28s ease, transform 0.28s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

#message-overlay.show {
  opacity: 1;
  transform: scale(1);
}

#message-text {
  font-family: 'Orbitron', sans-serif;
  font-weight: 900;
  font-size: clamp(24px, 5vw, 44px);
  letter-spacing: 5px;
  color: #00ff88;
  text-shadow: 0 0 20px #00ff88, 0 0 60px #00ff8866;
}

#message-sub {
  font-family: 'Orbitron', sans-serif;
  font-size: clamp(12px, 2vw, 18px);
  color: #aaffcc;
  margin-top: 10px;
  letter-spacing: 3px;
}

/* ── Instructions bar ─────────────────────────────────────────────────────── */

#instructions {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 4px 18px;
  width: 100%;
  padding: 0 4px;
}

.inst-item {
  font-size: 10px;
  color: #2a3a55;
  letter-spacing: 0.5px;
  white-space: nowrap;
}

.inst-item .highlight {
  color: #5599cc;
}

/* ── Mobile tweaks ────────────────────────────────────────────────────────── */

@media (max-width: 820px) {
  #title { letter-spacing: 2px; }
  #hud   { gap: 10px; }
  .inst-item { font-size: 9px; }
}

@media (max-width: 500px) {
  #hud .hud-item:last-of-type { display: none; } /* hide level on very small screens */
}
